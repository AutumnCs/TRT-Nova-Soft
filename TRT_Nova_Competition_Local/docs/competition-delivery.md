# 竞赛独立交付记录

更新：2026-09-09。当前分支 competition-local-cloud，基于 origin/main 的 0a774e38 新建；两套竞赛源码来自本地 PRD 当前实现，再合入审阅确认的兼容修复。原 main 工作目录、旧 LastScf/V2、PRD 工作树和已部署云资源未改动。

## 本轮范围

两个目录都自带前端、后端源码、依赖锁、配置模板、数据结构/种子与验证工具，不需要另一个目录。保留原项目 app 根布局，避免仅为交付改目录而大幅重写路径。云端从自己的 dist/scf 生成 5 个 ZIP 与单独客户端。

修正：天气开关与条件 SSM、AppID 唯一来源、SCF 公网/NAT 两种出口配置、设备码原值回退、部署凭据来源核验与历史轮换分离。新本地初始化拒绝覆盖已有库，日常启动不建表、不接管其他 MySQL。

产品保留与非默认内容见[重大变更登记](./material-feature-change-disclosure.md)，本机替代和云恢复缺口见[本地化登记](./localization-compromise-register.md)。

## 当前验证

- 本目录产品回归 493 项通过；云配置/SDK 等 28 项通过。
- 本地 HTTP + 实际新 MySQL 的 45 项检查通过，覆盖鉴权、双账号隔离、植宠、任务、日记/媒体。
- 云包真实独立 MySQL 的 16 组场景通过，外部服务由测试替身接管。
- 5 个 ZIP 的源码一致性、逐文件策略和哈希检查通过；5 份云端生产依赖锁的本次漏洞审计为 0。
- 不完整示例的发布检查返回 1，正确阻断。
- 仓库外两个干净副本分别安装自己的依赖；本地副本完成新库初始化、重复初始化拒绝、45 项接口、493 项回归与启停检查。云副本从头构建，并在本机 Node 20.19.0 下通过 493 项产品、28 项云/SDK、16 组数据库场景及全部包检查；不是腾讯云 Nodejs20.19 实际运行验收。
- 交付目录与各自干净副本逐文件字节一致；待交付路径不含 node_modules、.env.local、运行数据、私钥或构建产物。模式扫描命中的隐私拒绝测试字母序列和旧管理台文档中文占位符单独复核，不把测试夹具当真实凭据。

本地 HTTP 证据保存在本地版 .runtime/competition-smoke/；云演练每轮记录在 deployment/cloud-initial/state/verification/。独立副本测试证据交付在仓库外的“交付报告/2026-09-09_竞赛版验证”。这些运行产物不进 Git。

本轮原云副本记录批次 20260909-141010-098；干净云副本批次 20260909-142215-336。local-checks/result.json、cloud-checks/result.json 和 delivery-file-audit.json 是仓库外总入口。最终测试服务已停止，合成数据库、运行日志和失败记录保留供复核。

复现中修正了中文 MySQL 路径、PowerShell JSON 日期比较以及采集后台启动输出时的等待问题。后台启动直接调用，业务进程自己保存 stdout/stderr；验证脚本不把后台启动接入原生输出捕获管道。没有通过重置旧库或删除失败证据来恢复。

## 未执行、未验证

真实资源接入与部署、CAM/SSM、COS 删除效果、云 MySQL TLS、Function URL/自定义域名、真实微信/模型/天气、合法域名、体验版与真机均未验收。未迁移旧数据，未改变旧云流量，未启用 ingest/设备写入或发布管理后台。

本地新配置默认模型与天气关闭；完整真实 AI 演示需要另行配置并验证现有服务。旧机器专用 GUI/full-acceptance 脚本保留底稿，已从当前 npm 快捷入口移出，不算跨机器 GUI 验证。

历史凭据轮换按用户决定另行跟进；本次未执行、未声称完成。新目录不复制旧凭据，真正部署仍须确认本次引用的凭据有效且适用于目标隔离环境。没有提交、推送或对云资源执行写入。
