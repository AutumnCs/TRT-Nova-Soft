# 竞赛云端版交付说明

更新：2026-09-09。

本目录独立包含小程序、SCF 后端源码、数据库迁移、依赖锁、配置模板和验证工具。构建不依赖其他项目副本。

## 交付内容

| 位置 | 内容 |
|---|---|
| app.*、pages/、components/、services/ | 小程序与共享服务 |
| dist/scf/ | 后端部署源码 |
| deployment/cloud-initial/ | 函数构建、配置、迁移与验证工具 |
| data/knowledge/ | 已审核的初始知识内容 |
| docs/local-to-cloud-deployment-guide.md | 部署步骤与验收清单 |

安装依赖与构建命令见[README](../README.md)。云客户端需按目标环境生成，不直接使用占位地址。

## 自动验证

| 检查 | 结果 |
|---|---|
| 产品回归 | 499 项通过 |
| 云配置、附件与 HTTP 协议 | 32 项通过 |
| 隔离 MySQL 业务场景 | 18 组通过 |
| 五个 SCF 包入口、源码一致性和内容检查 | 通过 |
| 五份生产依赖锁漏洞审计 | 本次检查为 0 个已知漏洞 |

上述验证在本机 Node 20.19.0 下执行；MySQL 为真实隔离实例，微信、模型及云存储外部接口使用测试替身。这些结果不等同于真实云端或手机验收。

后续数据库链路调整已在本机 Node v24.16.0 通过 41 项云侧测试、五包构建/ZIP 校验及入口检查，并用包内连接模块完成旧实例只读直连；未重跑上述全部产品回归或完成云端验收。具体范围见[旧实例直连兼容验证](../deployment/cloud-initial/verification/2026-09-09-legacy-direct.md)。

实际目标新库已在现有实例上创建，继续使用现有账号；30 张表、1 个触发器、结构核验及事务回滚验证通过，包内连接模块已从本机连到新库。未创建账号、修改授权或迁移旧用户数据。见[实际新库准备验证](../deployment/cloud-initial/verification/2026-09-09-database-preparation.md)。

## 待完成

- 核对目标 SCF、MySQL 与小程序配置，推进隔离部署。
- CloudBase 代码和接口保留；已按后续要求恢复部署准备，图片、文档真实云存储联测仍待完成。
- 在隔离环境验证真实微信登录、基础业务接口及文本模型；附件生命周期待存储环境启用后验证。
- 核对合法域名，完成微信开发者工具、体验版和实体手机验证。
- 按部署计划确认回滚方式后再发布。

本交付不包含环境密钥、本机运行数据、个人开发者工具配置或用户数据。设备遥测接入及设备写入默认关闭；管理后台不包含在本次五个函数包中。

## 产品范围

保留四 Tab、植宠、任务、日记、知识、图片/文档、会话与摘要。正式任务需要用户确认，账号与植宠数据按归属隔离。新部署不自动迁移旧用户数据。

架构说明见[云端部署架构](./lastscf-deployment-reuse.md)。
