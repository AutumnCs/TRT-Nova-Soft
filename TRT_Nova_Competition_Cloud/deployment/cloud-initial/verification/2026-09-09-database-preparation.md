# 实际新库准备验证

日期：2026-09-09。范围：在既有 MySQL 实例上创建并初始化独立业务库，复用用户指定的现有账号。实例端点和账号值仅留在本地操作记录，不进入公开材料。

## 实际执行

- 22:35:05（北京时间）确认目标库不存在后创建，字符集 utf8mb4，排序规则 utf8mb4_unicode_ci。
- 复用 tools/migrate-database.mjs 的 bootstrap 入口与 12 份 SQL 文件，执行 47 条初始化语句。文件中有 45 条语句，额外两条为迁移器再次执行幂等的会话上下文建表语句。
- 建成 30 张 InnoDB 表，全部使用 utf8mb4_unicode_ci；创建 nova_cloudbase_media_delete 触发器。
- 结构核验 mode=verify 通过，核对关键列、文档 MIME 字段长度和触发器定义。
- 新库内插入合成用户及媒体账本，删除媒体后实际触发待清理状态；事务全部回滚，并确认测试记录为零。未调用云存储。
- 22:36:37 至 22:36:40，另开连接完成只读复核，并用 build/agent-scf/lib/db.js 的真实连接池连到新库，账号与库名符合目标。

创建/初始化和只读复核进程退出码均为 0。本次运行时为本机 Node v24.16.0，数据库为真实 MySQL 8.0.30-cynos；不是本机数据库替身。

## 数据状态

30 张表中，只有 plant_library 存在 4 条原有基线脚本的草稿资料；其余 29 张表为空。没有发布知识文章，也没有导入原库或本地测试库中的用户、植宠、会话、任务、图片和设备记录。

未运行 seed-reviewed-content.mjs。知识库上线内容不因结构初始化而自动获准发布。

## 边界

- 未创建数据库账号、执行 GRANT/REVOKE、改密码、变更网络/TLS 或数据库服务参数。
- 新旧业务分库，但使用同一现有账号，不声称账号权限隔离。
- 旧库列定义和索引元数据（151 列、64 个索引列条目）前后比对一致；账号授权前后一致。执行器没有将写连接指向旧库。
- 测试事务没有残留数据；自增序号可能因回滚消耗，不通过重置序号制造“从未写入”的假象。
- 本轮不部署 SCF，不触发定时清理，不执行 CloudBase 文件操作。SCF 出站、真实微信登录及完整业务联测仍未验收。

## 后续操作

现有目标库已经初始化，不再执行 bootstrap。需要检查时选择 verify；今后结构变更按受控增量路径执行。

本次未生成旧库数据备份或验证灾难恢复。若需要撤销新库，应先确认没有接入新流量或产生后续数据，再单独确定处理方式；不自动 DROP 或重建已存在的库。

当前五份函数 ZIP 未因本轮建库而改变；无需重新构建或上传才能证明这次数据库初始化。实际 SCF 部署仍是后续阶段。
