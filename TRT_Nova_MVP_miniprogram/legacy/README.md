## Legacy Code

This directory contains archived implementations that are no longer part of the active mini program runtime.

Current production path:
- mini program -> `auth-scf`
- mini program -> `api-scf`
- device -> OneNET -> `ingest-scf`

Archived here for historical reference only:
- `cloudfunctions/`
